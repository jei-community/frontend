import styled from '@emotion/styled';

export const S = {
  SideContainer: styled.div({
    display: 'flex',
    flexDirection: 'column',

    height: '100%',

    padding: '4rem 1.6rem',
    gap: '1.6rem',

    userSelect: 'none',
  }),
};
