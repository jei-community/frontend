import styled from '@emotion/styled';

export const S = {
  Container: styled.div({
    display: 'flex',
    flexDirection: 'column',

    gap: '3.2rem',
    padding: '3.2rem 1.6rem',

    userSelect: 'none',
  }),
};
