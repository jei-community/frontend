import styled from '@emotion/styled';

/** 아티클 스타일 */
export const S = {
  /** 아티클 페이지 콘텐츠 컨테이너 */
  Container: styled.div({
    width: '100%',
    height: 'fit-content',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: '2.4rem',
    padding: '2.4rem',
  }),
};
