import styled from '@emotion/styled';

/** 일일점검 메인 컨텐츠 컴포넌트 스타일 */
export const S = {
  /** 빈 컨텐츠일 때 영역 */
  EmptyContentContainer: styled.div({
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100vh',
    alignItems: 'center',
    justifyContent: 'center',
  }),
};
